// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.calendaria.team/services/sales/ent/sale"
	"gitlab.calendaria.team/services/sales/ent/saleitem"
)

// SaleItem is the model entity for the SaleItem schema.
type SaleItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// SaleID holds the value of the "sale_id" field.
	SaleID int64 `json:"sale_id,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID int64 `json:"product_id,omitempty"`
	// ProductName holds the value of the "product_name" field.
	ProductName string `json:"product_name,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity decimal.Decimal `json:"quantity,omitempty"`
	// UnitPrice holds the value of the "unit_price" field.
	UnitPrice decimal.Decimal `json:"unit_price,omitempty"`
	// Discount holds the value of the "discount" field.
	Discount decimal.Decimal `json:"discount,omitempty"`
	// Total holds the value of the "total" field.
	Total decimal.Decimal `json:"total,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SaleItemQuery when eager-loading is set.
	Edges        SaleItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SaleItemEdges holds the relations/edges for other nodes in the graph.
type SaleItemEdges struct {
	// Sale holds the value of the sale edge.
	Sale *Sale `json:"sale,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SaleOrErr returns the Sale value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SaleItemEdges) SaleOrErr() (*Sale, error) {
	if e.Sale != nil {
		return e.Sale, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: sale.Label}
	}
	return nil, &NotLoadedError{edge: "sale"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SaleItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case saleitem.FieldQuantity, saleitem.FieldUnitPrice, saleitem.FieldDiscount, saleitem.FieldTotal:
			values[i] = new(decimal.Decimal)
		case saleitem.FieldID, saleitem.FieldSaleID, saleitem.FieldProductID:
			values[i] = new(sql.NullInt64)
		case saleitem.FieldProductName:
			values[i] = new(sql.NullString)
		case saleitem.FieldCreatedAt, saleitem.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SaleItem fields.
func (_m *SaleItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case saleitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case saleitem.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case saleitem.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case saleitem.FieldSaleID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sale_id", values[i])
			} else if value.Valid {
				_m.SaleID = value.Int64
			}
		case saleitem.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				_m.ProductID = value.Int64
			}
		case saleitem.FieldProductName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field product_name", values[i])
			} else if value.Valid {
				_m.ProductName = value.String
			}
		case saleitem.FieldQuantity:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value != nil {
				_m.Quantity = *value
			}
		case saleitem.FieldUnitPrice:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field unit_price", values[i])
			} else if value != nil {
				_m.UnitPrice = *value
			}
		case saleitem.FieldDiscount:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field discount", values[i])
			} else if value != nil {
				_m.Discount = *value
			}
		case saleitem.FieldTotal:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field total", values[i])
			} else if value != nil {
				_m.Total = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SaleItem.
// This includes values selected through modifiers, order, etc.
func (_m *SaleItem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySale queries the "sale" edge of the SaleItem entity.
func (_m *SaleItem) QuerySale() *SaleQuery {
	return NewSaleItemClient(_m.config).QuerySale(_m)
}

// Update returns a builder for updating this SaleItem.
// Note that you need to call SaleItem.Unwrap() before calling this method if this SaleItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SaleItem) Update() *SaleItemUpdateOne {
	return NewSaleItemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SaleItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SaleItem) Unwrap() *SaleItem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SaleItem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SaleItem) String() string {
	var builder strings.Builder
	builder.WriteString("SaleItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("sale_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SaleID))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProductID))
	builder.WriteString(", ")
	builder.WriteString("product_name=")
	builder.WriteString(_m.ProductName)
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	builder.WriteString("unit_price=")
	builder.WriteString(fmt.Sprintf("%v", _m.UnitPrice))
	builder.WriteString(", ")
	builder.WriteString("discount=")
	builder.WriteString(fmt.Sprintf("%v", _m.Discount))
	builder.WriteString(", ")
	builder.WriteString("total=")
	builder.WriteString(fmt.Sprintf("%v", _m.Total))
	builder.WriteByte(')')
	return builder.String()
}

// SaleItems is a parsable slice of SaleItem.
type SaleItems []*SaleItem
